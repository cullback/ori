exports [U32]

U32 := {}.(
    add : U32, U32 -> U32
    sub : U32, U32 -> U32
    mul : U32, U32 -> U32
    div : U32, U32 -> U32
    mod : U32, U32 -> U32
    equals : U32, U32 -> Bool
    to_str : U32 -> Str
)
