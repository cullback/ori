module:
  type-alias main:
    Arrow:
      params:
        App List:
          Named Str
      ret:
        App Result:
          Named Str
          Named Str
  fn main(args):
    If : Result(Str, Str):
      scrutinee:
        QualifiedCall List.is_empty : Bool:
          Name args : List(Str)
      arm:
        pattern:
          Con True
        body:
          Call Ok : Result(Str, Str):
            StrLit "\n" : Str
      arm:
        pattern:
          Con False
        body:
          Call Ok : Result(Str, Str):
            QualifiedCall Str.concat : Str:
              MethodCall .unwrap => Result.unwrap : Str:
                receiver:
                  MethodCall .get => List.get : Result(Str, [OutOfBounds]):
                    receiver:
                      Name args : List(Str)
                    args:
                      IntLit 0 : U64
              StrLit "\n" : Str
